---
paths:
  - "secrets/**"
  - ".env*"
---

# Secrets -- Working Rules

## Secret storage

[GENERATED: Detect secret management — 1Password, vault, env vars, etc.]

## Never plaintext

All values in secret files must use references (e.g., op:// for 1Password).
Use Edit (not Write) to modify secret files — avoids losing existing references.

## .env file conventions

[GENERATED: Detect .env patterns — .env.example, .env.local, etc.]
