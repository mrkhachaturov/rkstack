{
  "description": "Guard npm publish, protect lockfiles from full overwrite",
  "detect": "stack.typescript OR stack.javascript",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [{ "type": "command", "command": "bash .claude/hooks/guard-destructive.sh" }]
      }
    ]
  },
  "permissions": {
    "deny": ["Bash(npm publish *)"]
  },
  "script": null
}
